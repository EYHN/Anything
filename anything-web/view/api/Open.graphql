fragment OpenFile on File {
  __typename
  path
  name
  icon
  mime
  dynamicIcon
  stats {
    accessTime
    creationTime
    modifyTime 
    size
  },
  ... on RegularFile {
    metadata
  }
}

query Open($path: String!) {
  file(path: $path) {
    ...OpenFile
  }
}